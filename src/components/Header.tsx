import React from 'react';
import './../index.css'; 

const Header: React.FC = () => {
  return (
    <header>
        <nav className="nav-links">
          <a href="/" >URL-XS</a>
        </nav>
    </header>
  );
};

export default Header;
